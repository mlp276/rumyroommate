const supertest = require('supertest');

describe('OP-01: Get Accounts', function () {
    it('gets all possible accounts with all of their attributes', function () {

    });

    it('gets all possible accounts specified by differing user ids', function () {

    });

    it('gets a specified number of accounts with all of their attributes', function () {

    });

    it('gets a specified number of accounts specified by differing user ids', function () {

    });
});

describe('OP-05: Get Listings', function () {
    it('gets at most five listings from the set of all created listings in the database', function () {

    });
});

describe('UC-03: Search Listings', function () {
    it('gets the preference value of the requesting user to match with all created listings', function () {

    });

    it('gets at most five listings based on the user\'s preference value', function () {

    });
});